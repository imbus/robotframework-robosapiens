"""
Add the paths to the changes in localized.en that must
be propagated to all translations. That is, the translations
must be updated only for these changes.

e.g. "keywords.ActivateTab.name"
"""

changeset = {
}
