"""
Add the paths to the changes in localized.en that should not
be propagated to all translations. That is, the translations
must not be updated due to these changes.

e.g. "keywords.ActivateTab.name"
"""

changeset = {
}
